{
  "name": "contributor-codex",
  "version": "0.9.1",
  "private": true,
  "description": "GitHub data compiled into visual cards, achievements, team leaderboards, and profiles — embeddable anywhere as server-rendered SVG",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build-font": "node scripts/build-font.js"
  },
  "dependencies": {
    "@vercel/firewall": "^1.2.1",
    "@boy-offi9-inc/reqkit": "^0.1.7"
  }
}
